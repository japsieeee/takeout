<!DOCTYPE html>
<html>
<%- include('./partials/head.ejs') %>
<body>
	<%- include('./partials/navbar.ejs') %>
	<section id="track">
		<h1 class="display-1 text-white bg-dark pb-5 fw-bold text-center"><%= message %></h1>

		<div class="alert alert-danger mx-4 my-4" role="alert">
		  <h4 class="alert-heading">Note</h4>
		  <hr>
		  <p>Please keep your <strong>order ID number</strong> it will help you to track your order whether it is on pending, processing, delivering and delivered. If you want to know more about customers right just <a href="/rights" class='text-decoration-none text-danger fw-bold'>click here</a></p>
		</div>

		<div class="container-fluid bg-dark p-0 m-0" style="height: 80vh">
			<div class='bg-dark p-5 bg-white text-dark'>
				<% results.forEach(result => { %>
					<p class="fs-3 fw-bold">Name: <span class='fw-normal'><%= result.buyer %></span></p>
					<p class="fs-3 fw-bold">Order ID Number: <span class='fw-normal'><%= result.oid %></span></p>
					<p class="fs-3 fw-bold">Status: <span class='badge bg-dark text-white'><%= result.status %></span></p>
					<hr>
					<p class="fs-3 fw-bold">Orders: </p>
					<% for (let i = 0; i < result.food.length; i++) { %>
						<p class="fs-2 d-flex justify-content-between"><span><%= result.food[i] + " x " + result.quantity[i] %></span></p>
					<% } %>

					<p class='fs-3 fw-bold d-flex justify-content-between border-top border-bottom border-2 border-dark py-3 my-5'><span>Total Sum</span><span><%= result.price %>.00</span></p>
				<% }) %>
			</div>
			<div class="d-flex justify-content-center my-4">
				<a href="/orders" class='btn btn-danger btn-lg'>Go Back</a>
			</div>
		</div>
	</section>
	<%- include('./partials/footer.ejs') %>
</body>
</html>